version: "3.8"

services:
  loki:
    container_name: loki
    image: grafana/loki:latest
    ports:
      - "3100:3100"
    command: -config.file=local-config.yaml
    volumes:
      - ./volumes/loki/etc:/etc/loki
